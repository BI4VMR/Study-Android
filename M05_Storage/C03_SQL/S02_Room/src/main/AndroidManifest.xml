<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_funny_256"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：进阶技巧 -->
        <activity
            android:name=".skills.TestUISkills"
            android:exported="false" />

        <!-- 示例界面：事务支持 -->
        <activity
            android:name=".transaction.TestUITransaction"
            android:exported="false" />

        <!-- 示例界面：数据库版本升级 -->
        <activity
            android:name=".upgrade.TestUIUpgrade"
            android:exported="false" />

        <!-- 示例界面：基本应用(KT) -->
        <activity
            android:name=".base.TestUIBaseKT"
            android:exported="false" />

        <!-- 示例界面：进阶技巧(KT) -->
        <activity
            android:name=".skills.TestUISkillsKT"
            android:exported="false" />

        <!-- 示例界面：事务支持(KT) -->
        <activity
            android:name=".transaction.TestUITransactionKT"
            android:exported="false" />

        <!-- 示例界面：数据库版本升级(KT) -->
        <activity
            android:name=".upgrade.TestUIUpgradeKT"
            android:exported="false" />
    </application>
</manifest>
