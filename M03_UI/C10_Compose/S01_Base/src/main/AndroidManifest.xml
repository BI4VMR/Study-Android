<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：数据交互 -->
        <activity
            android:name=".data.TestUIData"
            android:exported="false" />

        <!-- 示例界面：兼容性 - AndroidView -->
        <activity
            android:name=".compatible.TestUIAndroidView"
            android:exported="false" />

        <!-- 示例界面：兼容性 - ComposeView -->
        <activity
            android:name=".compatible.TestUIComposeView"
            android:exported="false" />

        <!-- TODO Demo -->
        <activity
            android:name=".appcenter.AppCenterActivity"
            android:exported="false" />
    </application>
</manifest>
