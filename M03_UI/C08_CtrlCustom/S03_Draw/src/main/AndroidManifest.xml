<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：点 -->
        <activity
            android:name=".point.TestUIPoint"
            android:exported="false" />

        <!-- 示例界面：直线 -->
        <activity
            android:name=".line.TestUILine"
            android:exported="false" />

        <!-- 示例界面：基本形状 -->
        <activity
            android:name=".shape.TestUIShape"
            android:exported="false" />

        <!-- 示例界面：路径 -->
        <activity
            android:name=".path.TestUIPath"
            android:exported="false" />

        <!-- 示例界面：文本 -->
        <activity
            android:name=".text.TestUIText"
            android:exported="false" />

        <!-- 示例界面：Shader -->
        <activity
            android:name=".shader.TestUIShader"
            android:exported="false" />

        <!-- 示例界面：基本应用(KT) -->
        <activity
            android:name=".base.TestUIBaseKT"
            android:exported="false" />

        <!-- 示例界面：点(KT) -->
        <activity
            android:name=".point.TestUIPointKT"
            android:exported="false" />

        <!-- 示例界面：直线(KT) -->
        <activity
            android:name=".line.TestUILineKT"
            android:exported="false" />

        <!-- 示例界面：基本形状(KT) -->
        <activity
            android:name=".shape.TestUIShapeKT"
            android:exported="false" />

        <!-- 示例界面：路径(KT) -->
        <activity
            android:name=".path.TestUIPathKT"
            android:exported="false" />

        <!-- 示例界面：文本(KT) -->
        <activity
            android:name=".text.TestUITextKT"
            android:exported="false" />

        <!-- 示例界面：Shader(KT) -->
        <activity
            android:name=".shader.TestUIShaderKT"
            android:exported="false" />
    </application>
</manifest>
