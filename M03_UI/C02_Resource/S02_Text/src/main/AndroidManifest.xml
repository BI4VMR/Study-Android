<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：字符串模板 -->
        <activity
            android:name=".template.TestUITemplate"
            android:exported="false" />

        <!-- 示例界面：复数 -->
        <activity
            android:name=".plurals.TestUIPlurals"
            android:exported="false" />

        <!-- 示例界面：多语言支持 -->
        <activity
            android:name=".i18n.TestUIMultiLanguage"
            android:exported="false" />

        <!-- 示例界面：字体 -->
        <activity
            android:name=".font.TestUIFont"
            android:exported="false" />

        <!-- 示例界面：基本应用(KT) -->
        <activity
            android:name=".base.TestUIBaseKT"
            android:exported="false" />

        <!-- 示例界面：字符串模板(KT) -->
        <activity
            android:name=".template.TestUITemplateKT"
            android:exported="false" />

        <!-- 示例界面：多语言支持(KT) -->
        <activity
            android:name=".i18n.TestUIMultiLanguageKT"
            android:exported="false" />

        <!-- 示例界面：字体(KT) -->
        <activity
            android:name=".font.TestUIFontKT"
            android:exported="false" />
    </application>
</manifest>
