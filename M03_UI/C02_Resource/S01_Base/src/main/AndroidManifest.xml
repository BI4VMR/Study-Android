<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 访问其他软件包的资源需要声明可见性 -->
    <queries>
        <package android:name="net.bi4vmr.study.ui.resource.base.res" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：资源索引 -->
        <activity
            android:name=".index.TestUIIndex"
            android:exported="false" />

        <!-- 示例界面：外部资源 -->
        <activity
            android:name=".remote.TestUIRemote"
            android:exported="false" />

        <!-- 示例界面：基本应用(KT) -->
        <activity
            android:name=".base.TestUIBaseKT"
            android:exported="false" />

        <!-- 示例界面：资源索引(KT) -->
        <activity
            android:name=".index.TestUIIndexKT"
            android:exported="false" />

        <!-- 示例界面：外部资源(KT) -->
        <activity
            android:name=".remote.TestUIRemoteKT"
            android:exported="false" />
    </application>
</manifest>
