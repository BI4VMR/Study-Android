<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_funny_256"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">

        <activity
            android:name="net.bi4vmr.study.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 示例界面：基本应用 -->
        <activity
            android:name=".base.TestUIBase"
            android:exported="false" />

        <!-- 示例界面：外观定制 -->
        <activity
            android:name=".style.TestUIStyle"
            android:exported="false" />

        <!-- 示例界面：超长内容滚动显示 -->
        <activity
            android:name=".scrolldisplay.TestUIScrollDisplay"
            android:exported="false" />

        <!-- 示例界面：自动文本尺寸 -->
        <activity
            android:name=".autosize.TestUIAutoSize"
            android:exported="false" />

        <!-- 示例界面：SpannableString -->
        <activity
            android:name=".span.TestUISpan"
            android:exported="false" />

        <!-- 示例界面：基本应用(KT) -->
        <activity
            android:name=".base.TestUIBaseKT"
            android:exported="false" />

        <!-- 示例界面：外观定制(KT) -->
        <activity
            android:name=".style.TestUIStyleKT"
            android:exported="false" />

        <!-- 示例界面：超长内容滚动显示(KT) -->
        <activity
            android:name=".scrolldisplay.TestUIScrollDisplayKT"
            android:exported="false" />

        <!-- 示例界面：自动文本尺寸(KT) -->
        <activity
            android:name=".autosize.TestUIAutoSizeKT"
            android:exported="false" />

        <!-- 示例界面：SpannableString(KT) -->
        <activity
            android:name=".span.TestUISpanKT"
            android:exported="false" />
    </application>
</manifest>
